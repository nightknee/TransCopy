image: nightknee/transcopy-bitbucket-image:1.0.0 

pipelines:
  default:
    - step:
        script: # Modify the commands below to build your repository.
#          - ./configure
          - make
#          - make test
